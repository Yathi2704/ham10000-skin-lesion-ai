# Training (Vast.ai, CUDA) + inference (M2 Air, MPS/CPU) share one file.
# On a Vast.ai PyTorch image torch/torchvision are preinstalled; pip will keep them.
torch>=2.9
torchvision>=0.24
pandas>=2.2            # HAM10000_metadata.csv
fastapi>=0.115
uvicorn[standard]>=0.30
grad-cam>=1.5          # PyPI name of pytorch-grad-cam (import: pytorch_grad_cam)
pillow>=10
numpy>=1.26
scikit-learn>=1.5
matplotlib>=3.8
seaborn>=0.13
qrcode[pil]>=7.4
python-multipart>=0.0.9

# Tests
pytest>=8
httpx>=0.27            # FastAPI TestClient backend (Phase B integration tests)
